# เจาะโครงสร้าง IIS

เอกสารความรู้สำหรับทีมพัฒนา อธิบายว่า IIS, Spring Boot, Apache Tomcat 11 และ React
แต่ละตัวคืออะไร ทำงานอย่างไร และเมื่อนำมาประกอบกันแล้วเป็นระบบเดียวได้อย่างไร

**เปิดอ่าน:** https://mellowgeeuy.github.io/iis-knowladge/

## ขอบเขต

เอกสารนี้อธิบาย **ชั้น deployment** คือส่วนที่รับ HTTP request จากเบราว์เซอร์
ตัดสินใจว่าจะตอบด้วยไฟล์ static บนดิสก์หรือ proxy ต่อไปยัง backend และบันทึก log ไว้

ส่วนที่ 01 ถึง 09 เรียกปลายทางว่า *upstream server* แบบกลาง ๆ
จึงใช้ได้โดยไม่ขึ้นกับว่า backend พัฒนาด้วยเทคโนโลยีใด
ส่วนที่ 10 ถึง 13 จึงค่อยลงรายละเอียดเฉพาะกรณีที่ backend คือ **Spring Boot บน Apache Tomcat 11**
และ frontend เป็น **React** ที่ build ด้วย Vite
โดยจำกัดเฉพาะค่าที่ต้องตั้งให้สอดคล้องกันระหว่างสามชั้น

เหตุที่ใช้ IIS คือระบบรันบน **Windows Server** ซึ่งมี IIS ติดมากับระบบปฏิบัติการอยู่แล้ว
มิใช่ข้อบังคับขององค์กร

สิ่งที่อยู่นอกขอบเขต ได้แก่ business logic ของแอปพลิเคชัน การออกแบบฐานข้อมูล
และการเขียนโค้ดภายในแต่ละเฟรมเวิร์ก

## เนื้อหา

เอกสารแบ่งเป็น 17 หน้า แสดงทีละส่วน

| ส่วน | หัวข้อ |
|---|---|
| — | ภาพรวมและขอบเขต |
| 01 | องค์ประกอบภายในของ IIS |
| 02 | หน่วยการจัดการทั้งสี่ |
| 03 | หน้าที่สองประการของ IIS |
| 04 | การติดตั้งองค์ประกอบที่จำเป็น |
| 05 | การสร้าง Application Pool และ Site |
| 06 | การกำหนดกฎใน web.config |
| 07 | การส่งข้อมูลผู้เรียกไปยังเซิร์ฟเวอร์ปลายทาง |
| 08 | การกำหนด HTTPS และ Binding |
| 09 | การตรวจสอบและวิเคราะห์ปัญหา |
| 10 | การเตรียม Spring Boot ให้ทำงานกับ IIS |
| 11 | Tomcat และการพัฒนาแบบแยกส่วน |
| 12 | การเตรียม React ให้ทำงานกับ IIS |
| 13 | Full Stack Architecture |
| — | อภิธานศัพท์ |
| — | แหล่งอ้างอิง |
| ◆ | Knowledge Graph |

ภายในมีแผนภาพ SVG 16 รูป ครอบคลุมทุกส่วนของเอกสาร โดยเส้นที่แทนการไหลของ request
เคลื่อนไหวตามทิศทางจริง และหยุดอัตโนมัติเมื่อผู้อ่านตั้งค่าลดการเคลื่อนไหวในระบบปฏิบัติการ

## Knowledge Graph

หน้าสุดท้ายรวมทุกสิ่งที่เอกสารพูดถึงเป็นกราฟเดียว **54 node · 65 relationship**
แบ่งสีตามหมวด IIS · Java · React · สถาปัตยกรรม กดที่ node เพื่อดูว่าต่อกับอะไรบ้าง
ด้วย relationship ชื่ออะไร และชี้ที่เส้นเพื่ออ่านชื่อ relationship ทีละเส้น

ตำแหน่งของ node คำนวณด้วย force layout แบบ deterministic นอกเบราว์เซอร์แล้วตรึงพิกัดไว้
จึงตรวจได้จริงว่าไม่มีกล่องทับกันและไม่มีเส้นพาดผ่านกล่องใด

เกณฑ์ที่ใช้ตัดสินว่า “ข้อมูลถึงกันหมด” คือ Weakly Connected Components
ซึ่งเอกสารของ Neo4j ระบุว่าใช้ตัดสินได้ว่าเครือข่ายเชื่อมถึงกันทั้งหมดหรือไม่
กราฟนี้รันแล้วได้ **1 component ขนาด 54 node**

## ดาวน์โหลดเป็น Markdown

ปุ่ม **ดาวน์โหลดเป็น .md** ที่มุมซ้ายล่างของเมนู ให้ไฟล์ [`iis-knowladge.md`](iis-knowladge.md)
ซึ่งเป็นเอกสารทั้งฉบับในรูปแบบ Markdown — หัวข้อ ตาราง บล็อกโค้ด คำอธิบายรูป
และแหล่งอ้างอิงพร้อมลิงก์ ครบทั้ง 17 หน้า สำหรับนำไปต่อยอดในที่อื่น


## แหล่งอ้างอิง

เนื้อหาทุกส่วนอ้างอิงจากเอกสารทางการของ Microsoft, Spring Boot, Apache Tomcat, Vite,
React Router, W3C และ Neo4j ที่เปิดอ่านเนื้อหาเต็มแล้ว
รวม 36 รายการ แสดงเป็นตารางในหน้าแหล่งอ้างอิง ชื่อเรื่องอยู่บรรทัดบน ลิงก์อยู่บรรทัดล่าง

ชื่อ site พาธของไฟล์ หมายเลขพอร์ต และพาธปลายทางทั้งหมดเป็นค่าตัวอย่างที่กำหนดขึ้น
มิใช่ค่าที่ตรวจวัดหรือดึงมาจากระบบจริง

## โครงสร้างไฟล์

```
.
├─ index.html         เอกสารทั้งฉบับ ไฟล์เดียวจบ ไม่มี dependency ภายนอกนอกจากฟอนต์
├─ iis-knowladge.md   เอกสารฉบับ Markdown สำหรับดาวน์โหลดไปต่อยอด
├─ .nojekyll          ปิดการประมวลผลด้วย Jekyll ของ GitHub Pages
└─ README.md
```

`index.html` ฝังทั้ง CSS และ JavaScript ไว้ในไฟล์เดียว
โหลดเฉพาะฟอนต์จาก Google Fonts ได้แก่ Anuphan, Kanit และ Source Code Pro

## การเปิดใช้งาน GitHub Pages

1. เข้าไปที่ **Settings → Pages** ของ repository
2. ที่ **Source** เลือก **Deploy from a branch**
3. เลือก branch `main` และโฟลเดอร์ `/ (root)` แล้วกด **Save**
4. รอสักครู่ เว็บจะเผยแพร่ที่ `https://mellowgeeuy.github.io/iis-knowladge/`

## การแสดงผล

ใช้ชุดสี Earth tone ชื่อ Adventure — เขียวป่า ขาวนวล และเทาอมเขียว
รองรับทั้งธีมสว่างและธีมมืดตามการตั้งค่าของเครื่องผู้อ่าน
และปรับการแสดงผลได้ถึงความกว้างระดับโทรศัพท์มือถือ

เนื้อหาแบ่งเป็นหมวดในเมนูด้านซ้าย ได้แก่ เริ่มต้น · IIS สามกลุ่มย่อย · Java · React · Full Stack · อ้างอิง
ใต้เมนูมีปุ่มดาวน์โหลด .md และปุ่มเปิดหน้า Knowledge Graph
บนจอแคบเมนูจะยุบเป็นปุ่มสารบัญ · เปลี่ยนหน้าได้ด้วยเมนู ปุ่มด้านล่าง หรือปุ่มลูกศรซ้ายและขวา
ลิงก์แบบ `#s04` เปิดตรงไปยังส่วนนั้นได้ทันที
